# Copy to .env and adjust
LOG_LEVEL=info
SEEDS_FILE=seeds.txt
MAX_CONCURRENCY=2

# Proxy Configuration (choose one)
# PROXY_URL=http://user:pass@proxy.example.com:8080
# USE_FREE_PROXIES=true
# PROXY_LIST=proxies.txt

# ProxyScrape Integration (recommended for free proxies)
# USE_PROXYSCRAPE=true
# PROXYSCRAPE_PROTOCOL=http # Options: http, socks4, socks5
# PROXYSCRAPE_TIMEOUT=5000 # Timeout in milliseconds (1000-10000)
# PROXYSCRAPE_COUNTRY=US # Country code (US, GB, DE, etc.) or 'all'

# Tiered ProxyScrape (best reliability - uses multiple country tiers)
# USE_TIERED_PROXYSCRAPE=true
# PROXYSCRAPE_COUNTRIES=US,GB,CA,AU,DE # Comma-separated country codes in priority order
# PROXYSCRAPE_PROTOCOL=http # Options: http, socks4, socks5
# PROXYSCRAPE_TIMEOUT=5000 # Timeout in milliseconds (1000-10000)
